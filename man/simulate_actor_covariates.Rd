% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_covariates.R
\name{simulate_actor_covariates}
\alias{simulate_actor_covariates}
\title{Simulate exogenous actor covariates}
\usage{
simulate_actor_covariates(
  senders,
  receivers,
  covariate_names,
  time_points = NULL,
  sd = 1,
  rho = 0,
  seed = NULL
)
}
\arguments{
\item{senders}{Character vector of sender actors.}

\item{receivers}{Character vector of receiver actors.}

\item{covariate_names}{Character vector naming the covariates to simulate.}

\item{time_points}{Optional numeric vector of strictly increasing time stamps
for time-varying covariates. When omitted, static covariates are returned.}

\item{sd}{Standard deviation of the innovation noise.}

\item{rho}{AR(1) coefficient used when \code{time_points} is supplied. Must be
in (-1, 1).}

\item{seed}{Optional integer to make the simulation reproducible.}
}
\value{
A list with two elements: \code{sender_covariates} and
\code{receiver_covariates}. Each element is either a wide data.frame (static
case) or a tidy data.frame with columns \code{actor}, \code{time},
\code{covariate}, and \code{value} (dynamic case).
}
\description{
Create simple exogenous covariate structures for senders and receivers. The
function can return static values (one row per actor) or time-stamped
processes (one row per actor and time point) that follow independent AR(1)
dynamics.
}
\examples{
sender_cov <- simulate_actor_covariates(
  senders = letters[1:3],
  receivers = LETTERS[1:2],
  covariate_names = c("activity", "recency"),
  time_points = seq(0, 4),
  rho = 0.6,
  sd = 0.2,
  seed = 123
)
str(sender_cov)
}
